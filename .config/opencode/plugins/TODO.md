# TODO

## Task Completion Monitor
- session x already prompted, skipping
  - only gets prompted once and terminates, even though there's no password given
  - should keep getting prompted, until there is a password

## User Input Notifier
